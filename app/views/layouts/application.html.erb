<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <%= yield :head %>
</head>
<body>	
    <% if controller?('hroniks') && action?('show') || controller?('static_pages') && action?('index')  %>
		<%= render :partial => 'shared/hronik_suggest', :locals => { :@hronik => Hronik.new } %>
		<% end %>
        <div class="wrapper">
            <%= render 'layouts/header' %>
		    <%= yield %>
            <div class="push"></div>
        </div>
        <div class="container footer">
            <%= render 'layouts/footer' %>
        </div>
		<%= render 'shared/metrika' %>
    </body>
</html>

<body>
